# 端到端测试清单

## 测试环境
- 前端地址: http://localhost:5173
- 后端地址: http://localhost:8088
- 测试账号: 2021001 / password123

---

## 1. 登录功能

### 测试步骤
1. 打开前端页面，进入登录界面
2. 输入学号: `2021001`，密码: `password123`
3. 点击"登录"按钮
4. 观察页面跳转和响应

### 预期结果
- [ ] 登录成功，页面跳转到主页
- [ ] 显示用户信息（学生姓名、班级）
- [ ] Session 创建成功
- [ ] 错误密码时显示"学号或密码错误"提示

---

## 2. 签到状态查询

### 测试步骤
1. 登录后进入主页
2. 查看签到状态区域
3. 调用 API: `GET /api/checkin/status`
4. 观察返回数据

### 预期结果
- [ ] 返回 `checkedIn` 布尔值
- [ ] 返回 `count` 当日签到次数
- [ ] 未签到时显示"未签到"状态
- [ ] 已签到时显示"已签到"状态和时间

---

## 3. GPS 定位功能

### 测试步骤
1. 进入签到页面
2. 点击"获取位置"按钮
3. 允许浏览器定位权限
4. 观察位置获取结果

### 预期结果
- [ ] 浏览器弹出定位权限请求
- [ ] 成功获取经纬度坐标
- [ ] 显示当前位置信息（地址文字）
- [ ] 定位失败时显示友好错误提示
- [ ] 坐标格式正确（latitude, longitude 为 double 类型）

---

## 4. 人脸识别功能

### 测试步骤
1. 进入签到页面
2. 点击"开始人脸识别"按钮
3. 允许摄像头权限
4. 对准人脸进行识别
5. 调用 API: `POST /api/face/detect`

### 预期结果
- [ ] 摄像头正常开启，显示视频流
- [ ] 检测到人脸时显示绿色边框
- [ ] 人脸识别通过后显示"验证成功"
- [ ] 未检测到人脸时提示"请正对摄像头"
- [ ] 人脸识别失败时提示"人脸识别未通过"

---

## 5. 签到提交功能

### 测试步骤
1. 完成人脸识别和 GPS 定位
2. 点击"签到"按钮
3. 调用 API: `POST /api/checkin/verify`
4. 观察签到结果

### 预期结果
- [ ] 签到成功显示"签到成功"提示
- [ ] 返回 `faceVerified: true`
- [ ] 返回 `locationVerified: true`
- [ ] 返回 `checkedIn: true`
- [ ] 签到记录写入数据库
- [ ] 签到状态更新为"已签到"

---

## 6. 防重复签到

### 测试步骤
1. 完成一次成功签到
2. 再次尝试签到
3. 检查 Redis 中的防重标记
4. 观察系统响应

### 预期结果
- [ ] 第二次签到被拒绝
- [ ] 返回 `400` 错误码
- [ ] 提示"今日已签到，不可重复签到"
- [ ] Redis 中存在 `checkin:today:{id}:{date}` 键
- [ ] 该键 TTL 约为 23 小时

---

## 7. AI 对话功能

### 测试步骤
1. 进入 AI 对话页面
2. 输入消息: "我今天签到了吗？"
3. 观察 SSE 流式响应
4. 调用 API: `GET /api/ai/chat-stream?message=...`

### 预期结果
- [ ] 消息发送成功
- [ ] AI 逐字流式输出回复（每字符 20ms 间隔）
- [ ] 回复内容相关且有意义
- [ ] SSE 格式正确: `data: {char}\n\n`
- [ ] 结束时发送 `data: [DONE]\n\n`
- [ ] 对话记录保存到 chat_history 表

---

## 8. 签到记录查询

### 测试步骤
1. 进入签到记录页面
2. 调用 API: `GET /api/checkin/records?page=0&size=10`
3. 观察分页数据
4. 点击翻页按钮

### 预期结果
- [ ] 返回分页数据结构
- [ ] 包含 `content` 数组（签到记录列表）
- [ ] 包含 `totalPages`、`totalElements`、`currentPage`、`size`
- [ ] 记录按 `checkTime DESC` 排序
- [ ] 翻页功能正常工作
- [ ] 每条记录包含：studentId, studentName, roomNumber, checkTime, status

---

## 9. 主题切换功能

### 测试步骤
1. 进入设置页面
2. 点击"深色模式"切换按钮
3. 观察页面主题变化
4. 刷新页面后观察主题是否保持

### 预期结果
- [ ] 主题切换动画流畅
- [ ] 深色模式：背景色变为深色，文字变为浅色
- [ ] 浅色模式：恢复正常配色
- [ ] 主题选择保存到 localStorage
- [ ] 刷新页面后主题保持不变
- [ ] 所有页面元素主题一致

---

## 10. Session 持久化

### 测试步骤
1. 正常登录系统
2. 关闭浏览器标签页
3. 重新打开前端地址
4. 检查登录状态
5. 调用 API: `GET /api/auth/check`

### 预期结果
- [ ] 关闭标签页后重新打开，仍然保持登录状态
- [ ] `GET /api/auth/check` 返回 `loggedIn: true`
- [ ] 返回完整的用户信息（studentId, studentName, className）
- [ ] Session 有效期符合预期
- [ ] 勾选"记住我"后 Session 持久时间更长
- [ ] 未登录时调用 `/api/auth/check` 返回 `loggedIn: false`（非 401）

---

## 测试结果汇总

| 序号 | 测试项 | 状态 | 备注 |
|------|--------|------|------|
| 1 | 登录功能 | ⬜ 待测试 | |
| 2 | 签到状态查询 | ⬜ 待测试 | |
| 3 | GPS 定位功能 | ⬜ 待测试 | |
| 4 | 人脸识别功能 | ⬜ 待测试 | |
| 5 | 签到提交功能 | ⬜ 待测试 | |
| 6 | 防重复签到 | ⬜ 待测试 | |
| 7 | AI 对话功能 | ⬜ 待测试 | |
| 8 | 签到记录查询 | ⬜ 待测试 | |
| 9 | 主题切换功能 | ⬜ 待测试 | |
| 10 | Session 持久化 | ⬜ 待测试 | |

---

## 测试完成标准
- 所有 10 项测试全部通过
- 无阻断性 Bug
- 核心流程（登录 → 签到 → 查看记录）完整可用
